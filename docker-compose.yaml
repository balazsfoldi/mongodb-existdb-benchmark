version: '3.8'

services:
  mongodb:
    image: mongo:8
    container_name: mongodb
    ports:
      - "27017:27017"

  existdb:
    image: existdb/existdb:latest
    container_name: existdb
    ports:
      - "8080:8080"